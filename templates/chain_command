import * as app from "../app.js"

export default new app.Command<"all">()
  .setName("{{ name }}")
  .setDescription("The {{ name }} command")
  .setChannelType("all")
  .onRun(async (message) => {
    // todo: code here
    return message.send("{{ Name }} command is not yet implemented.")
  })
