import * as app from "../app.js"

export default new app.Command({
  name: "{{ name }}",
  description: "The {{ name }} command",
  channelType: "all",
  async run(message) {
    // todo: code here
    return message.send("{{ name }} command is not yet implemented.")
  }
})